// const { default: plugin } = require("tailwindcss");

// const { default: autoprefixer } = require("autoprefixer");

module.exports = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
  };
  
  